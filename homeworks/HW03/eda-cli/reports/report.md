# EDA-отчет

Исходный файл: `another.csv`

Строк: **10**, столбцов: **8**

## Качество данных (эвристики)

- Оценка качества: **0.00**
- Макс. доля пропусков по колонке: **70.00%**
- Возможные выбросы:
	**Столбец: score, значение: 200.0 , тип выброса: toobig**
- В каких столбцах много пропусков( >30.0% ): **sex**
- Слишком мало строк: **True**
- Слишком много колонок: **False**
- Слишком много пропусков: **True**

- Сколько константных столбцов: **1**

## Колонки

См. файл `summary.csv`.

## Пропуски

См. файлы `missing.csv` и `missing_matrix.png`.

## Корреляция числовых признаков

См. `correlation.csv` и `correlation_heatmap.png`.

## Категориальные признаки

См. файлы в папке `top_categories/`.

## Гистограммы числовых колонок

См. файлы `hist_*.png`.
